/**
 * The controller package contains classes that interface between the user, the model and the view.
 * The controllers listen for user input, feed that input to the model, and update the view
 * accordingly. The game has two primary controllers, one which manages the menu view,
 * and the other which manages the game view.
 */

package controller;